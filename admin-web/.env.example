# ─── Database (shared with storefront) ───
TURSO_DATABASE_URL=https://your-db-xxxxx.sqld.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# ─── Auth ───
AUTH_JWT_SECRET=change-me-random-32-chars-minimum

# ─── Desktop Sync ───
# Static token for desktop app sync (NOT a user JWT).
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DESKTOP_SYNC_TOKEN=your-desktop-sync-token
