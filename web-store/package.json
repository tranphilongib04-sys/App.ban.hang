{
  "name": "tbq-homie-deployment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cd .. && npx netlify dev --offline --dir web-store",
    "test": "node -e \"console.log('No automated tests configured for web-store.')\"",
    "build": "node -e \"console.log('No build step required for web-store (static).')\"",
    "migrate": "node scripts/fix_db_schema.js",
    "generate-secret": "node scripts/setup-helper.js generate-secret",
    "test-api": "node test-api.js",
    "test-webhook": "node scripts/test-webhook.js",
    "quick-test": "./scripts/quick-test.sh",
    "serve": "npx serve ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "node-fetch": "^2.7.0"
  }
}
